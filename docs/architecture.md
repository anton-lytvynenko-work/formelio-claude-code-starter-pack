# Architecture

> Filled as the project grows. `/plan` updates this for STRUCTURAL plans; `/implement`'s final task reconciles it against actual code.

## Summary

{{PROJECT_DESCRIPTION}}

## Tech Stack

- **Language / Stack:** {{STACK}}
- **Runtime:** {{RUNTIME_VERSION}}
- **Package manager:** {{PACKAGE_MANAGER}}

## Directory Map

_Generated by `/implement` Phase 7.5 into `.claude/rules/custom/project.md` (always current). High-level summary below._

```
src/                   Application code
docs/prds/             Source-of-truth PRDs
docs/plans/            Implementation plans
docs/diagrams/         Mermaid diagrams
hinq-zno-design-system/         HINQ ZNO design system
.claude/               Claude Code automation
```

## Key Flows

_Add a row per end-to-end user flow. `/plan` adds these for STRUCTURAL plans with UI changes._

| Flow | Description | Diagram |
|------|-------------|---------|
| _(none yet)_ | | |

## Diagrams

_Add a row per Mermaid diagram. `/plan` adds these for STRUCTURAL plans._

| Diagram | Type | File |
|---------|------|------|
| _(none yet)_ | | |

## Decisions

Architectural decisions live in `docs/adr/`. Write an ADR when a choice would surprise a future reader (e.g. why we chose library A over B, why we deferred caching, why we used an unusual pattern).
